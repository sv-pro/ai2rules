//! # cli-harness
//!
//! Terminal entrypoint for the CLI agent harness. Wires the TUI, model loop,
//! and approval UX over `agent-core`.
//!
//! Status: skeleton (E0). Implementation tracked in PLAN.md **E9**.

fn main() {
    println!(
        "cli-agent harness — skeleton (E0). See PLAN.md and docs/harness-architecture.md for the plan."
    );
}
