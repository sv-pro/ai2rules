{
  "name": "hero-mcp",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Focused, governable MCP capability: generate a house-style blog hero image via the agy CLI.",
  "bin": { "hero-mcp": "dist/index.js" },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "tsc && node --test dist/lib.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "sharp": "^0.33.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
